import DashboardPlaceholderPage from "@/Components/Dashboard/DashboardPlaceholderPage";

export default function TutorDashboardFinancesPage() {
  return (
    <DashboardPlaceholderPage
      title="Finances"
      description="This page is reserved for tutor earnings and future payout history. The layout is in place now so the finances module can be added without redesigning the dashboard shell."
    />
  );
}
