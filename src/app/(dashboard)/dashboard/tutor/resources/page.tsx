import DashboardPlaceholderPage from "@/Components/Dashboard/DashboardPlaceholderPage";

export default function TutorDashboardResourcesPage() {
  return (
    <DashboardPlaceholderPage
      title="Resources"
      description="This page can later surface tutor notes, materials, and reusable lesson assets. For now it serves as a role-aware placeholder inside the shared dashboard layout."
    />
  );
}
