import DashboardPlaceholderPage from "@/Components/Dashboard/DashboardPlaceholderPage";

export default function DashboardSessionsPage() {
  return (
    <DashboardPlaceholderPage
      title="Sessions"
      description="This section will hold upcoming, completed, and cancelled sessions for the signed-in user. The tutor dashboard shell is ready, and we can plug the real sessions module into this route next."
    />
  );
}
